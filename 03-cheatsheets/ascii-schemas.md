# Bibliotheque de schemas ASCII reutilisables

> 15 schemas "whiteboard" prets a dessiner en salle ou copier dans tes fiches.
> Tous testes sur VS Code / terminal.

---

## 1. Hub-Spoke classique

```
                    HUB (shared services)
                    ┌────────────────────┐
                    │  Azure Firewall    │
                    │  VPN Gateway       │
                    │  DNS Private       │
                    │  Monitoring        │
                    └─────────┬──────────┘
                              │
              ┌───────────────┼───────────────┐
              │               │               │
              ▼               ▼               ▼
        ┌──────────┐    ┌──────────┐    ┌──────────┐
        │  SPOKE   │    │  SPOKE   │    │  SPOKE   │
        │   Prod   │    │  PreProd │    │   Dev    │
        │          │    │          │    │          │
        │  VNet A  │    │  VNet B  │    │  VNet C  │
        └──────────┘    └──────────┘    └──────────┘

  VNet peering entre HUB et chaque SPOKE
  Aucun peering SPOKE-to-SPOKE (traffic via HUB)
```

---

## 2. Multi-region Active-Active avec Front Door

```
       ┌─────────────────────┐
       │     USERS (global)  │
       └──────────┬──────────┘
                  │ HTTPS
                  ▼
       ┌─────────────────────┐
       │  Azure Front Door   │
       │  Premium (global)   │
       │  + WAF + caching    │
       └────┬────────────┬───┘
            │            │
     health │            │ health
     probe  │            │ probe
            ▼            ▼
    ┌─────────────┐  ┌─────────────┐
    │  REGION 1   │  │  REGION 2   │
    │  West EU    │  │  North EU   │
    │             │  │             │
    │ App Service │  │ App Service │
    │     ZRS     │  │     ZRS     │
    │             │  │             │
    │ Cosmos DB  ◄┼──┼► Cosmos DB  │
    │ (multi-w)   │  │ (multi-w)   │
    └─────────────┘  └─────────────┘
```

---

## 3. Identity flow hybride (Azure AD + on-prem)

```
  ┌──────────────┐                    ┌──────────────┐
  │  ON-PREM AD  │                    │  ENTRA ID    │
  │              │                    │  (Azure AD)  │
  │  users       │                    │              │
  │  groups      │                    │  cloud apps  │
  │  policies    │                    │  conditional │
  │              │                    │  access      │
  └──────┬───────┘                    └──────┬───────┘
         │                                   │
         │     Azure AD Connect              │
         ├──────────────────────────────────►│
         │  (hash sync or pass-through)      │
         │                                   │
         │              SSO                  │
         │◄──────────────────────────────────┤
         │       (federated or cloud)        │
         │                                   │
  ┌──────▼───────┐                    ┌──────▼───────┐
  │  on-prem     │                    │  Azure apps  │
  │  apps        │                    │  M365, apps  │
  └──────────────┘                    └──────────────┘
```

---

## 4. Availability Zones intra-region

```
     REGION West Europe
     ┌─────────────────────────────────────────┐
     │                                         │
     │   ZONE 1        ZONE 2        ZONE 3    │
     │   ┌───┐         ┌───┐         ┌───┐     │
     │   │VM1│         │VM2│         │VM3│     │
     │   └─┬─┘         └─┬─┘         └─┬─┘     │
     │     │             │             │       │
     │     └─────────────┼─────────────┘       │
     │                   │                     │
     │              ┌─────▼────┐               │
     │              │ Azure LB │               │
     │              │   Std    │               │
     │              └──────────┘               │
     │                                         │
     └─────────────────────────────────────────┘

    SLA 99.99% compute (avec 2+ zones)
    Latence inter-zone < 2 ms
```

---

## 5. SQL DR avec auto-failover group

```
     PRIMARY REGION              SECONDARY REGION
     (West Europe)               (North Europe)

     ┌───────────────┐           ┌───────────────┐
     │  SQL DB/MI    │           │  SQL DB/MI    │
     │  Business     │──sync─────▶  Business     │
     │  Critical     │  replica  │  Critical     │
     │  (primary)    │           │  (secondary)  │
     │               │           │  read-only    │
     └───────┬───────┘           └───────┬───────┘
             │                           │
             │     Listener endpoint     │
             │◄─────────(single URI)────▶│
             │                           │
             ▼                           ▼
     ┌───────────────────────────────────┐
     │  Auto-failover group              │
     │  - Automatic on incident          │
     │  - RPO < 1 min (sync)             │
     │  - RTO < 1h                       │
     └───────────────────────────────────┘
```

---

## 6. Management Groups + Azure Policy hierarchy

```
                    ┌─────────────────────┐
                    │  ROOT MG (Tenant)   │
                    │  Policy : tag req   │
                    │  Policy : region EU │
                    └──────────┬──────────┘
                               │
                ┌──────────────┼──────────────┐
                ▼              ▼              ▼
          ┌──────────┐   ┌──────────┐   ┌──────────┐
          │  MG PROD │   │ MG DEV   │   │ MG SBOX  │
          │  +Policy │   │ +Policy  │   │ +Policy  │
          │  no pub  │   │ free     │   │ expires  │
          │  IP      │   │          │   │ 30 days  │
          └────┬─────┘   └────┬─────┘   └────┬─────┘
               │              │              │
           subscription    subscription  subscription
               │              │              │
           resource          resource    resource
           groups            groups      groups

  Policy hiritees : chaque niveau applique les siennes + parents
```

---

## 7. Serverless event-driven architecture

```
   ┌─────────┐       ┌─────────────┐
   │  USER   │──────▶│  API        │
   │         │       │  Gateway    │
   └─────────┘       └──────┬──────┘
                            │
                            ▼
                     ┌─────────────┐
                     │  Azure      │
                     │  Functions  │
                     │  (consumpt) │
                     └──────┬──────┘
                            │
                  publish   │
                            ▼
                     ┌─────────────┐
                     │  Service    │
                     │  Bus Topic  │
                     └──┬──────────┘
                        │
            ┌───────────┼───────────┐
            ▼           ▼           ▼
      ┌─────────┐ ┌─────────┐ ┌─────────┐
      │ Function│ │ Function│ │ Function│
      │ Email   │ │ CRM sync│ │ Audit   │
      └─────────┘ └─────────┘ └─────────┘

  Scale-to-zero sur toutes les Functions
  Cost = usage pur
```

---

## 8. Private Endpoint vs Service Endpoint

```
  ┌─── PRIVATE ENDPOINT ──────────────────────────┐
  │                                               │
  │    VNet                         Azure Service │
  │   ┌─────┐    private IP         ┌──────────┐  │
  │   │ VM  │───10.0.1.4────────────│ SQL DB   │  │
  │   │     │    via PE             │ blob etc │  │
  │   └─────┘                       └──────────┘  │
  │                                               │
  │  Traffic : reste dans backbone Microsoft      │
  │  DNS : private DNS zone                       │
  │  Public endpoint : peut etre desactive        │
  │                                               │
  └───────────────────────────────────────────────┘

  ┌─── SERVICE ENDPOINT ──────────────────────────┐
  │                                               │
  │    VNet                         Azure Service │
  │   ┌─────┐    public IP          ┌──────────┐  │
  │   │ VM  │──via backbone─────────│ SQL DB   │  │
  │   │     │    (not internet)     │ blob etc │  │
  │   └─────┘                       └──────────┘  │
  │                                               │
  │  Traffic : backbone mais via public endpoint  │
  │  DNS : public                                 │
  │  Public endpoint : tjs actif                  │
  │                                               │
  └───────────────────────────────────────────────┘
```

---

## 9. RPO/RTO timeline

```
                    INCIDENT
                        │
       ─────────────────▼──────────────────────▶ TIME
                        │
             RPO        │         RTO
          ◄─────────────┼─────────────────►
             │                       │
        last backup              service
        available                 restored

  Exemple :
    RPO = 1h → last backup 1h before incident
    RTO = 4h → 4h after incident, service back up

  Donnees perdues = entre last backup et incident
  Downtime = entre incident et service restored
```

---

## 10. Authentication methods pyramid

```
                    ╱╲
                   ╱  ╲
                  ╱ FIDO2╲  ← phishing-resistant
                 ╱────────╲    (best for admins)
                ╱ Windows   ╲
               ╱  Hello       ╲ ← phishing-resistant
              ╱────────────────╲  (Windows fleet)
             ╱  Authenticator   ╲
            ╱  app + number      ╲ ← passwordless
           ╱   matching            ╲  (mobile users)
          ╱──────────────────────────╲
         ╱  Password + Authenticator   ╲ ← MFA
        ╱   (classic)                   ╲  (baseline)
       ╱─────────────────────────────────╲
      ╱   Password + SMS                  ╲ ← DEPRECATED
     ╱    (legacy)                          ╲  (avoid)
    ╱─────────────────────────────────────────╲
   ╱    Password only                          ╲  ← NEVER
  ╱      (insecure)                              ╲   (never use)
 ╱─────────────────────────────────────────────────╲

  Top = recommande Microsoft
  Bottom = jamais en production
```

---

## 11. Data platform layers

```
  ┌──────────────────────────────────────────────────┐
  │  LAYER 5 — PRESENTATION                          │
  │  Power BI | Fabric reports | custom app          │
  └──────────────┬───────────────────────────────────┘
                 │
  ┌──────────────▼───────────────────────────────────┐
  │  LAYER 4 — SERVING                               │
  │  Synapse SQL Pool | Cosmos DB | SQL DB           │
  └──────────────┬───────────────────────────────────┘
                 │
  ┌──────────────▼───────────────────────────────────┐
  │  LAYER 3 — TRANSFORM                             │
  │  Databricks | Synapse Spark | Data Factory       │
  │  Fabric (unified)                                │
  └──────────────┬───────────────────────────────────┘
                 │
  ┌──────────────▼───────────────────────────────────┐
  │  LAYER 2 — STORAGE                               │
  │  Data Lake Storage Gen2 (parquet/delta)          │
  └──────────────┬───────────────────────────────────┘
                 │
  ┌──────────────▼───────────────────────────────────┐
  │  LAYER 1 — INGESTION                             │
  │  Event Hubs | IoT Hub | Data Factory | Files    │
  └──────────────────────────────────────────────────┘

  Flux bottom-up : ingestion → stockage → transform → serving → UI
```

---

## 12. AKS cluster anatomy

```
     ┌──────────────────────────────────────────────┐
     │  AKS CLUSTER                                 │
     │                                              │
     │   ┌─────────────────────────────────┐        │
     │   │ CONTROL PLANE (managed by MS)   │        │
     │   │   api-server | etcd | scheduler │        │
     │   │   controller-manager | cloud-ctl│        │
     │   └──────────┬──────────────────────┘        │
     │              │                               │
     │   ┌──────────▼─────────┐                     │
     │   │   SYSTEM NODEPOOL  │  (CoreDNS, Calico) │
     │   │   min 1 Standard   │                     │
     │   └────────────────────┘                     │
     │                                              │
     │   ┌────────────────────┐                     │
     │   │   USER NODEPOOL    │  (your workloads)  │
     │   │   VMs + spot + GPU │                     │
     │   └────────────────────┘                     │
     │                                              │
     │   Integrations : ACR | AAD | Monitor | KV   │
     │                                              │
     └──────────────────────────────────────────────┘
```

---

## 13. Load balancing decision

```
   QUESTION : je dois load-balance. Quoi choisir ?
                          │
                          ▼
            ┌─────────────────────────┐
            │ Traffic HTTP(S) ?       │
            └────┬────────────────┬───┘
                 │ Oui            │ Non (TCP/UDP)
                 ▼                ▼
         ┌───────────────┐  ┌──────────────────┐
         │ Scope ?       │  │ Azure LB         │
         └──┬──────────┬─┘  │ (L4, internal    │
            │ Global   │    │  or external)    │
            │          │    └──────────────────┘
            ▼          ▼ Regional
     ┌───────────┐  ┌─────────────────┐
     │ Front Door│  │ App Gateway     │
     │ (CDN+WAF) │  │ (WAF + path     │
     │           │  │  routing)       │
     └───────────┘  └─────────────────┘

  NB : Traffic Manager = DNS-based (legacy pour certains cas cross-region)
```

---

## 14. Cost optimization tiers par service

```
   COMPUTE (du moins cher au plus cher)
   ┌─────────────────────────────────────────┐
   │ Spot VMs      ────► -90% (interruptible)│
   │ Dev/Test      ────► -55%                │
   │ Reserved 3yr  ────► -72%                │
   │ Reserved 1yr  ────► -40%                │
   │ Pay-as-you-go ────► 0% (baseline)       │
   │ Burstable B-  ────► moins cher mais     │
   │ series             credits epuisables   │
   └─────────────────────────────────────────┘

   STORAGE tiers (Blob)
   ┌─────────────────────────────────────────┐
   │ Hot           ────► 1x (reference)      │
   │ Cool          ────► 0.5x + read fees    │
   │ Cold          ────► 0.3x + read fees    │
   │ Archive       ────► 0.08x + retrieval   │
   │                    (hrs rehydration)    │
   └─────────────────────────────────────────┘
```

---

## 15. Migration 7R framework (CAF)

```
       ┌────────────────────────────────────┐
       │     MIGRATION STRATEGY CHOICE       │
       └─────────────────┬──────────────────┘
                         │
    ┌────────────────────┼────────────────────┐
    │                    │                    │
    ▼                    ▼                    ▼
 ┌───────┐          ┌────────┐          ┌────────┐
 │ KEEP  │          │ CHANGE │          │ DROP   │
 └───┬───┘          └───┬────┘          └───┬────┘
     │                  │                   │
   ┌─┴──────┐         ┌─┴───────┐         ┌─┴─────┐
   ▼        ▼         ▼         ▼         ▼       ▼
┌────┐ ┌──────┐  ┌──────┐  ┌──────┐   ┌────┐ ┌────┐
│Rehst│ │Replat│  │Refact│  │Rearch│   │Retr│ │Repur│
│     │ │      │  │      │  │      │   │    │ │chase│
│lift │ │PaaS  │  │code  │  │deep  │   │RIP │ │SaaS │
│&    │ │minor │  │small │  │rewrit│   │app │ │repl │
│shift│ │chg   │  │chg   │  │      │   │    │ │     │
└────┘ └──────┘  └──────┘  └──────┘   └────┘ └────┘

  Les 7 R :
  1. Rehost    (lift & shift)
  2. Replatform (minor changes)
  3. Refactor  (code changes)
  4. Rearchitect (major rewrite)
  5. Rebuild   (from scratch)
  6. Retire    (delete)
  7. Repurchase (SaaS)
```

---

## Bonus — Symboles a connaitre

```
 ┌─────┐   Service / Resource          (⚪)   Utilisateur / Actor
 │     │
 └─────┘

   ─────►   Flux synchrone              [X]   Service deprecie
   ═════►   Flux secure (encrypted)
   ─ ─ ─►   Flux asynchrone             ☁    Internet / public
   ◄───►    Bi-directional

 ┌═════┐   Service critique prod      (!)   Attention / piege
 │     │
 └═════┘
```

---

## Conseils d'usage

> [!TIP] Ces schemas sont faits pour etre **reproduits en salle au whiteboard**, pas projetes.
>
> - Utilise **3 couleurs** : noir (structure), bleu (flux data), rouge (securite/piege)
> - Dessine **progressivement** pendant l'explication (pas d'un coup)
> - Demande aux eleves de **redessiner** dans leurs notes
> - Pour les **reference architectures polies**, passe en Excalidraw/draw.io

### Outils recommandes pour versions finales

- **Excalidraw** : https://excalidraw.com/ (web, gratuit, look dessine a main)
- **draw.io / diagrams.net** : https://app.diagrams.net/ (icones Azure officielles)
- **Mermaid** : dans markdown, pour les flowcharts/sequences
